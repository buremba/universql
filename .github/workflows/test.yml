name: Release

on:
  push:

jobs:
  pip:
    name: Build and Test
    runs-on: ubuntu-latest
    environment: deploy
    steps:
      - name: Check out the repository
        uses: actions/checkout@v3
        with:
          fetch-depth: 2
      - name: Set up Python
        uses: actions/setup-python@v3
        with:
          python-version: "3.11"
      - name: Upgrade pip
        run: |
          pip install --constraint=.github/workflows/constraints.txt pip
          pip --version
      - name: Install Poetry
        run: |
          pip install --constraint=.github/workflows/constraints.txt poetry
          poetry --version
          poetry install
      - name: Test project
        env:
          SNOWFLAKE_CONNECTIONS: ${{ secrets.SNOWFLAKE_CONNECTIONS_BASE64 }}
        run: |
          mkdir -p ~/.snowflake
          echo $SNOWFLAKE_CONNECTIONS | base64 --decode > ~/.snowflake/connections.toml
          echo 'olumm2'
          echo $SNOWFLAKE_CONNECTIONS | base64 --decode
          wc -l ~/.snowflake/connections.toml
          poetry run pytest tests/integration/*